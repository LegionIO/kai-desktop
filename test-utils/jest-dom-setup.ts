/**
 * Jest-DOM matcher registration. Loaded only by the component-test config
 * (`vitest.component.config.ts`) so the unit and integration slices keep a
 * clean global namespace.
 */

import '@testing-library/jest-dom/vitest';
